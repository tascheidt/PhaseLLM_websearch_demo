def process(messageprompt):
    # You can still keep the import statements if you need them later
    # import json
    # import os
    # from phasellm.agents import WebSearchAgent
    # from itertools import chain
    # from apikeys import openai_key, google_api_key, search_id

    results2 = "Step 2 worked"
    return results2